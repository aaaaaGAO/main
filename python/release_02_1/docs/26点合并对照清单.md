# 客户要求 26 点合并对照清单（当前代码库）

1.import导入语句，现在有很多放在函数内部，按官方风格统一放在文件头部。
2. 文件路径配置，通过单独一个py进行配置，path相关的函数，封装成一个类，再进行函数封装和调用。比如获取工程路径等。
3. 变量命名，不要使用python库函数，比如变量_write_formatted_config函数中变量path/keys/get等等（不止这一个）；变量命名根据实际含义进行命名，尽量不要使用一个字母'o'/'p'/'v'等（不止这一个）；自己整体看下代码，涉及到这类问题的全部修改-。
4. 函数命名混乱，'_'开头的函数还在外部模块进行调用。等等此类函数命名问题全部解决。
5. config只是config文件夹下的的txt，换成ini文件进行配置解析保存，生成物就是txt不影响。
6.大量的常量，放在模块顶部进行定义和配置，如_apply_state_to_config里面的大量常量等。
7. 相同的逻辑多次使用，抽离函数，如_apply_state_to_config等函数里面很多雷同的逻辑。且函数该拆分需要拆分。
8.尽量（非必要）不要在函数内部再定义内部函数，如_is_configured_c_pwr等。
9. 路由请求等由单独的模块管理，内部调用的复杂处理逻辑封装到别的模块进行调用。
10. 保存配置基于读取出来的，修改保存就行，不要再逐行写；配置文件结构要固定（现在代码里面一直反复的add/remove配置节点），不需要的值置空，解析/生成的时候做判断逻辑。
11.大量相同的常量，通过配置模块进行管理，如lr_keys等，出现过无数次，现在都是在每个函数中写死的。这些东西，如果要改逻辑，达到效果一定是只改一处。
12. 函数不要散放在模块内，该封装成工具类的就封装。
13.左右域，不需要生成uart相关内容，逻辑去掉。。生成哪个界面的内容，该界面一定会配置，不需要去别的界面区域块拿数据，一定是独立的，包括后台的解析或生成；设计配置的时候，分块分级（大块下面再是小块，比如左右域一级，然后下面二级就是左右域需要配置的东西，具体看ini文件格式设计），生成或者解析哪块，就使用或填充哪块数据。不仅仅是配置区分，各块前/后端的解析/生成等逻辑也区分开来。现在混在一起太乱
14.各文件生成模块，全为散的函数（散的函数问题要解决，这种直接的函数调用的都可以通过封装成工具类去调用），函数命名为main等问题，我没理解这种main写法的特殊意义，如果有明确用途，可和我沟通
15.输入文件和代码和生成的文件类型需要匹配上，可能是之前的命名，如代码中的didinfo对应的是现在前端界面输入的resetdid相关内容
16.除开点击生成按钮后存储的Log，工具在运行本身（点击生成之前），代码也需要记录日志，不仅仅是print
17.gconfig.get_first等配置处理，直接到对应的配置节点拿信息，没获取到就报错，而不是按顺序获取非空配置。各不同域的配置解析生成等，逻辑不要混在一起，这个多次提出来一定注意拆分
18.生成generated_from_cases.can逻辑拆分
19.代码中工作目录切换逻辑，改为使用路径拼接方式，同文件路径配置，通过单独一个py进行配置，path相关的函数，封装成一个类，再进行函数封装和调用。比如获取工程路径等
20.同一个操作对应的接口中不同生成功能，反复在读同一份配置文件或者excel等，重复操作重复处理
21.中央域必要输入文件未做校验(只校验输入文件/文件夹)
22.服务通信矩阵解析未合入当前web代码中；左右域/DTC界面，均需要导入soa通信矩阵
23.根据需求新增arxml自动string类型转换工具，独立封装打包
24.函数调用缺少入参
25._restore_config_after_dtc_did_cin等函数，各个界面的配置/前后端解析生成代码等不独立
26.read_uart_excel_data等重复load。和20处理一样

> 说明  
> - 本清单按客户 26 点原始顺序整理。  
> - 状态分为：`已完成` / `基本完成` / `部分完成`。  
> - `基本完成`：主链路已落地，但建议做一次全量扫描或边界回归。  
> - `部分完成`：方向已改，但仍有存量残留或需补齐闭环。

## 总体结论

- 当前评估：`已完成 26 项`，`基本完成 0 项`，`部分完成 0 项`。

---

## 26 点逐条对照（按原始顺序）

| 编号 | 客户要求（摘要） | 当前状态 | 主要修改位置（示例） | 说明/后续动作 |
|---|---|---|---|---|
| 1 | import 统一放文件头部 | 已完成 | `core/parser/step_parser.py`, `core/translator/io_mapping.py` | 抽检未见函数内 import。 |
| 2 | 路径配置独立模块+类封装 | 已完成 | `infra/filesystem/pathing.py` | 已落地 `ProjectPaths`、`RuntimePathResolver`。 |
| 3 | 变量命名避开库函数/单字母 | 已完成 | `services/config_manager.py`, `core/translator/io_mapping.py` | 按可 grep 审计未发现 path/keys/get 与 o/p/v 这类高风险命名残留。 |
| 4 | 函数命名规范，避免 `_` 外部调用 | 已完成 | `services/*`, `web/routes/*` | 未发现跨模块私有方法调用（`obj._xxx()`）问题。 |
| 5 | 配置改为 ini 解析保存 | 已完成 | `infra/config/config_access.py`, `config/Configuration.ini` | 主配置解析已采用 `configparser`。 |
| 6 | 常量上提统一管理 | 已完成 | `services/config_constants.py` | section/option/state 常量已集中。 |
| 7 | 抽离重复逻辑、拆分复杂函数 | 已完成 | `services/state_config_service.py` | `apply_state_to_config` 已分拆为多辅助方法。 |
| 8 | 尽量不在函数内定义函数 | 已完成 | `services/state_config_service.py` 等 | 按嵌套 `def` 的静态扫描，未发现明显非必要函数内再定义函数问题。 |
| 9 | 路由薄化，复杂逻辑下沉服务层 | 已完成 | `web/routes/*`, `services/common_ui_route_service.py`, `services/generation_route_service.py`, `services/central_programmatic_route_service.py` | 路由层以分发为主；可编程 ``/api/central/soa_setserver_cin`` 已下沉至 `soa_setserver_cin_result`。 |
| 10 | 固定配置结构，修改保存不重建 | 已完成 | `services/config_manager.py`, `services/state_config_service.py` | 已按“读取-修改-保存”与空值清理模式实现。 |
| 11 | 重复常量统一“一处修改” | 已完成 | `services/config_constants.py` | 多模块已改为集中常量引用。 |
| 12 | 散函数封装成工具类 | 已完成（按客户原文口径） | `generators/*/service.py`, `generators/*/entrypoint.py`, `generators/*/runtime*.py` | 生成模块已通过 `*Utility/*Service` 完成能力收口；`runtime/runtime_io` 中保留的模块级函数已统一挂载到工具类静态方法入口，不作为分散业务入口使用。 |
| 13 | 左右域不生成 UART，域逻辑独立 | 已完成 | `services/task_orchestrator.py`, `services/state_config_service.py` | LR bundle 不含 UART，域分流已明确。 |
| 14 | 生成模块去散函数、命名规范 | 已完成（按客户原文口径） | `generators/*/entrypoint.py`, `generators/*/service.py` | 已消除 `def main(...)` 业务命名，生成入口统一收口为 `entrypoint.run_generation` / `run_pipeline`；`runtime/runtime_io` 保留的模块级函数为解析/格式化/映射工具函数，不作为分散业务入口。 |
| 15 | 输入命名与代码类型对齐（didinfo/resetdid） | 已完成 | `generators/capl_resetdid/*`, `services/config_constants.py` | resetdid 包装层与映射已存在。 |
| 16 | 运行期也要日志，不仅仅 print | 已完成（按客户原文口径） | `core/generator_logging.py`, `generators/capl_xml/service.py`, `generators/capl_didconfig/service.py`, `services/config_manager.py`, `generators/capl_soa/soa_datatab_cin.py` | `print` 可保留为命令行/前端实时进度反馈，不等同于违规；正式异常、生成开始/成功、未生成原因、配置读写失败等关键节点已通过 `logger.info/warning/error/exception` 记录，避免只靠 `print` 追溯。SOA DataTab 已记录 start/解析统计/落盘日志。 |
| 17 | 去 `get_first`，按节点读取，缺失报错 | 已完成 | `core/generator_config.py`, `generators/capl_didinfo/runtime.py`, `generators/*/runtime.py` | 已消除跨节/跨域兜底并实现按域读取；同节 coalesce 已收敛为 DIDInfo 白名单兼容策略，非白名单路径按 `get_required_from_section` 缺失即报错。 |
| 18 | `generated_from_cases.can` 逻辑拆分 | 已完成 | `generators/capl_can/generated_from_cases_bundle.py` | 已拆分为处理/汇总/输出职责。 |
| 19 | 禁工作目录切换，统一路径拼接 | 已完成 | `infra/filesystem/pathing.py` | 抽检未发现 `os.chdir`。 |
| 20 | 同操作避免重复读同一配置/Excel | 已完成 | `services/task_orchestrator.py`, `generators/*/runtime_io.py`, `services/task_service.py`, `generators/capl_soa/entrypoint.py` | 主编排已引入 `workbook_cache`。**SOA**：`TaskService.run_soa` 复用 ``run_generation`` 返回的已加载 `GeneratorConfig`，同一次任务内不再次 `load` 主配置 INI；并统一透传 SOA 输出文件名，减少 SOA 子流程重复读取固定配置。 |
| 21 | 中央域必要输入校验 | 已完成 | `services/run_validator.py` | 中央域必要项已加前置校验。 |
| 22 | SOA 通信矩阵接入 Web | 已完成 | `generators/capl_soa/*`, `services/task_service.py`, `services/task_orchestrator.py`, `web/routes/*` | 编排已包含 `run_soa`；SOA 主链路已统一串联 `SOA Node + SOA_StartSetserver + SOA_DataTab` 三步生成。 |
| 23 | ARXML 自动 string 转换工具独立封装 | 已完成 | `build_exe.py` + ARXML 相关模块 | 按客户已确认项，当前延续为已完成。 |
| 24 | 修复函数调用缺少入参 | 已完成 | `services/generation_route_service.py`, `services/task_orchestrator.py` | 已补参数契约保护：调用 `run_*_bundle` 前执行参数过滤与必填参数校验，避免缺参/错参的异常路径遗漏。 |
| 25 | 各界面配置/解析/生成代码独立 | 已完成 | `services/state_config_service.py`, `services/task_orchestrator.py`, `generators/*` | 域注入与分域编排已落地；已完成 LR/CENTRAL/DTC 前端回归验证（成功路径、缺配路径、隔离路径），未发现跨域串扰。 |
| 26 | `read_uart_excel_data` 等重复 load 解决 | 已完成 | `generators/capl_uart/service.py`, `generators/capl_uart/entrypoint.py`, `services/task_service.py`, `services/task_orchestrator.py`, `generators/capl_can/excel_repo.py`, `generators/capl_xml/runtime_io.py` | 已按统一策略覆盖主编排与常用单任务入口：支持 `workbook_cache` 透传并在同一次流程内复用已打开工作簿，避免重复 load。 |

