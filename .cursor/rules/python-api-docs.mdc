---
description: Python 规范 + 客户原始需求（第一版 1–12、第二版 1–14）防回归
globs: "**/*.py"
alwaysApply: true
---

# Python 代码与架构约束（`python/release_02_1`）

修改或新增代码时，除下列「文档与 API 说明」外，须**同时遵守**文 `docs/用户要求修改点_第一版实施计划.md`、`docs/用户要求修改点.md` 中的客户原始条款；**不要重新引入**已收敛的问题（与命名审计 CI、既有架构一致）。

## 全篇禁令（两版重复强调的）

- **不新增**以 **`def _`** 为对外可被其它模块调用的 **API**；下划线表示模块私有时不得跨包引用。
- **非必要**不在**函数体内部**再 `def` 嵌套业务函数；可提取为**模块级**或**类方法**。
- **路径**：集中由 `pathing` / `RuntimePathResolver` 等约定入口处理；**禁止** `os.chdir` 式切工作目录，用**路径拼接**；业务路径不各处手写魔法串。
- **配置**：在**规定节/域**用显式 `get` / `get_required_from_section`；**禁止**恢复「`get_first` 式」跨多节**偷第一个非空**的兜底新路径（已有 `coalesce_options_in_section` 仅**同节多键**并列）。
- **重复 I/O**：同一次操作内避免反复打开同一 **ini/Excel/Workbook**；复用**已有缓存/上下文**或向上层传**已读对象**。
- **散函数与工具类**：业务逻辑不长期堆在模块顶层「一堆函数」；**适合封装的**收进 `*Service` / `*Utility` 等，路由层保持**薄**。

---

## 客户原始需求 · 第一版（实施计划 1–12，摘要）

1. **`import` 在文件头部**，与官方风格一致；确需延迟导入的少数场景注明原因，避免在普通函数里随意 `import`。
2. **路径配置**集中在专门模块；路径相关**封装为类**再对外提供方法/函数（如工程根、输出目录等）。
3. **命名**：避免与**库名/内建**冲突或误导（如滥用 `path`/`keys`/`get` 作变量名）；**禁止**无含义单字母如 `o`/`p`/`v` 承载业务；见命名审计/CI。
4. **命名**：**禁止**从**外部模块**调用本模块的 **`_` 开头**「私用」函数；对外入口须是公开、稳定名字。
5. **配置格式**：`config` 下主配置用 **`.ini` 解析与写回**；说明性/生成物为 **`.txt` 的**不改变客户交付形态。
6. **常量**提到**模块顶部**或**集中配置模块**；不在巨型函数中淹没大块魔法字面量。
7. **重复逻辑**抽函数；**过长函数**按职责**拆分**。
8. 尽量**不要**在函数内再写内部函数；除非有闭包硬需求且极短。
9. **Web 路由**只做分发与入参，**复杂处理**到 **service/其它模块**。
10. **配置保存**：基于**已读出**的结构**就地修改后写回**；**节结构尽量固定**；不用的项**置空**，靠解析侧判断；避免到处 **`add_section`/`remove` 打补丁**式流程。
11. 多处共用的**键名/常量**（如原 `lr_keys` 类）**只在一处**（配置/常量模块）定义，**改一处全局生效**。
12. 模块内**不随意堆放**可复用过程；**该工具类就工具类**。

---

## 客户原始需求 · 第二版（1–14，摘要）

1. **域独立**：如左右域不跑与 **UART** 相关的生成/编排；**只处理当前界面/域**该配的块，**禁止**为省事从**别的界面节**偷数据；**INI 分块分级**与**前后端**解析/生成**按块对应**，不混在一大坨里。
2. **生成器**：减少**散在的顶层函数**；用 **类/服务** 封装；**业务入口**不要滥用无含义的 **`def main(...)` 名**（若保留须能说明职责或改名）。
3. **命名与类型一致**：如 **didinfo** 与界面 **resetdid** 等**语义对齐**；**输入/输出**文件类型、扩展名与真实生成物一致。
4. **日志**：除「生成后落盘 Log」外，**工具运行/请求处理过程**中也要用 **logging**（等），**不要仅靠 `print` 当正式日志**（调试 print 与业务可观测分离）。
5. 配置在**对应当前域/节**取；缺则**显式失败**；**各域的 load/解析/落盘**不混写在一个「万能函数」里。
6. **`generated_from_cases` 的 CAN 生成**逻辑已**拆在** `generated_from_cases_bundle` 等；**新逻辑**不往回塞巨型单函数里。
7. 同第一版路径条款：**不 chdir**、**路径类与单文件 path 策略**、集中封装。
8. 同「全篇禁令」**重复读**：一次接口/操作内不反复读**同一**配置或 Excel（见 **14**）。
9. **中央域**对**必要输入**做校验（**不仅是**有文件/有文件夹）。
10. **SOA 服务矩阵**：Web/左右域/DTC 流程中该导入、编排到的**要合在现有架构里**，不半套留在脚本外（与第一版**路由+服务**分工一致）。
11. **ARXML 字符串工具**：客户要求**已单独打包**的**不破坏、不乱迁**；此处**不随需求展开新范围**时勿改其边界。
12. 调用函数时**补全**所需**上下文/入参**，**禁止**为省事传空或缺关键参数靠下游猜。
13. **各界面**的**配置保存/恢复、前后端**逻辑**解耦**；**不要**新写类似「一个大函数里按界面分支揉在一起」的 `_restore_*` 式耦合名与行为（旧名已收敛，**勿回归**）。
14. 与 **8** 同：**UART** 等**重复 `read_*_excel` / 重复 open** 能合并的合并、能缓存的传 Workbook/缓存句柄。

---

## 文件头（每个 `.py` 最上方）

在模块 docstring 中说明（中文，与 `core/case_filter.py` 等现有风格一致）：

- 本文件**负责什么**、在整体流程中的**角色**
- 若有：**关键依赖**、**表头/配置约定**、**与哪些模块协作**
- 2～12 行为宜

`#!/usr/bin/env python3` 与 `# -*- coding: utf-8 -*-` 若文件已有则保留。

## 函数 / 方法（`def`）

对**可维护的、对外或模块内承担职责的**函数与**公开方法**编写 docstring，须包含：

- **功能**（一句说明做什么）
- **参数**：名称、含义、可选/默认、单位或取值约定
- **返回值**：类型语义与特殊值（如 `None` 表示什么）

**类**应有简短 docstring；**`@property` / `@staticmethod` 等**与实例方法同要求。

**可省略 docstring 的少数情况**：`__all__` 旁一行 re-export、纯转发的一行元编程、行数极少且名已自解释的内部 `_` 辅助；其余不偷懒。

## 执行方式

- **新写或经手修改的**代码：上述客户条款 + 文档要求**在当次 PR/改动范围内**满足；**避免新增**违反第一版、第二版正文的点。
- **历史未动文件**：全库无需单次补完；**触达时顺带**收拢。

与既有代码风格冲突时，以**同目录邻近文件**与 **`docs/架构.txt`** 为准。
